<div class="container">
	<div class="row">
		<div class="col-md-6">
			<div>
				<% @hashtags.each do |tag| %>
					<hr/>
						<p class="text-info"><%= link_to tag.name, hashtag_path(tag) %></p>
					<hr/>
				<% end %>
			</div>
		</div>
	</div>
</div>